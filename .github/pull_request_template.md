## Summary

## Changes

## Checklist
- [ ] `npm run typecheck` passes
- [ ] `npm test` passes
- [ ] `npm run build` passes
- [ ] Updated `CHANGELOG.md` under `## [Unreleased]`
- [ ] If `.grome/` directory model changed, updated `buildInjection()` and the table in `CLAUDE.md`
